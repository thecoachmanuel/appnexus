# AppNexus

AppNexus is a comprehensive platform for building and managing applications.

## Getting Started

1. Copy `.env.example` to `.env`
2. Run `npm install`
3. Run `npm run dev` to start the development server.
